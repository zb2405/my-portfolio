#################################################
# REDIRECT ROOT DOMAIN → WWW (CANONICAL DOMAIN)
#################################################
server {
    listen 80;
    server_name zakibawade.in;

    return 301 https://www.zakibawade.in$request_uri;
}

#################################################
# MAIN WEBSITE
#################################################
server {
    listen 80 default_server;
    server_name www.zakibawade.in;

    root /var/www/html;
    index index.html;

    #################################################
    # SECURITY HEADERS
    #################################################

    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    add_header Content-Security-Policy "
        default-src 'self';
        img-src 'self' data:;
        style-src 'self' 'unsafe-inline';
        script-src 'self';
        connect-src 'self';
        font-src 'self';
        object-src 'none';
        base-uri 'self';
        frame-ancestors 'none';
    " always;

    #################################################
    # RATE LIMITING
    #################################################

    limit_req zone=req_limit burst=20 nodelay;

    #################################################
    # ALLOW ONLY SAFE METHODS
    #################################################

    if ($request_method !~ ^(GET|HEAD)$) {
        return 444;
    }

    #################################################
    # BLOCK HIDDEN FILES
    #################################################

    location ~ /\.(?!well-known) {
        deny all;
        access_log off;
        log_not_found off;
    }

    #################################################
    # STATIC SITE
    #################################################

    location / {
        try_files $uri $uri/ =404;
    }

    autoindex off;
}
