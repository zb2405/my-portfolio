<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>zakibawade.in — Coming Soon</title>

  <!-- Umami Analytics (proxied via nginx) -->
  <script
    defer
    src="/umami/script.js"
    data-website-id="2f6b943e-a310-45b8-b974-7dfc6a652293">
  </script>

  <style>
    * {
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;
    }

    body {
      background:#0f172a;
      color:#e2e8f0;
      display:flex;
      justify-content:center;
      align-items:center;
      height:100vh;
      text-align:center;
    }

    .container {
      max-width:700px;
      padding:40px;
    }

    h1 {
      font-size:3rem;
      margin-bottom:1rem;
      color:#38bdf8;
    }

    p {
      font-size:1.2rem;
      opacity:.85;
      margin-bottom:2rem;
    }

    .badge {
      display:inline-block;
      padding:8px 14px;
      border-radius:999px;
      background:rgba(56,189,248,.1);
      border:1px solid rgba(56,189,248,.4);
      font-size:.9rem;
      margin-top:12px;
    }

    .visitor-counter {
      margin-top:18px;
      font-size:1rem;
      color:#22d3ee;
      letter-spacing:.08em;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>🚧 Website Coming Soon</h1>

    <p>
      Infrastructure deployed via Terraform + Ansible.<br/>
      Portfolio launching soon.
    </p>

    <div class="badge">
      Nginx • Cloudflare Tunnel • Umami Analytics
    </div>

    <!-- PAGEVIEW COUNTER -->
    <div id="visitor-count" class="visitor-counter">
      Views: --
    </div>
  </div>

    <script defer src="/visitors.js"></script>
</body>
</html>
